# PotatoFlow v1.5.36

- 恢复 ASS 弹幕烧录能力，每个直播间可独立开启，设置会持久化并同步到录播处理配置。
- 正常投稿和“仅录制不投稿”现在都可先生成 ASS、烧录进视频，再继续上传或封面封装；烧录失败时保留原录播与独立 ASS。
- 新增跨线程、跨进程的 ASS 烧录队列，多个分段同时收尾时串行使用 FFmpeg，任务页可区分“等待烧录”和“正在烧录”。
- 新增“投稿中心”，可按 B站账号和状态分页查看历史稿件，或通过 BVID 精确定位稿件及分P。
- 支持使用服务器上已完成 ASS 烧录的视频替换指定分P。换源前强制二次确认 BVID，重新读取远端稿件，并保留其他分P、标题、简介、标签和封面。
- 换源上传纳入 B站全局上传队列，投稿中心会持久记录等待、上传、提交和失败状态，轮询接口不暴露服务器视频绝对路径。
