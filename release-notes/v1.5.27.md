# PotatoFlow v1.5.27

- AI 只选择重要事件并返回可核验的原文关键词，不再手写时间字符串，从根源避免将“21 分钟”错写成 `00:21`。
- 程序使用证据关键词回到完整 XML，定位第一条同时匹配的弹幕，再默认向前补偿 8 秒的观众反应延迟。
- 只有原文和关键词均可在采样弹幕中核验的事件才会生成时间点；程序按 XML 原始秒数排序、检查录制时长，并统一输出 `MM:SS` 或 `HH:MM:SS`。
