# PotatoFlow v1.6.18

## 内置组件精简与目录整理

- 精简内置录制组件，只保留实际构建和运行需要的 Rust workspace、迁移文件、锁定文件及许可证，减少无用源码和镜像构建上下文。
- 将内置录制组件目录统一为 `recorder-core`，应用目录统一为 `potatoflow-app`，清理旧项目名称在界面、日志和内部模块中的历史痕迹。
- 保留旧目录配置、投稿引擎配置和上传线路缓存的自动迁移，升级后无需手动搬迁现有配置与数据。
- 简化 Python 依赖安装流程，并修复深色模式下编码器回退提示不清晰的问题。
