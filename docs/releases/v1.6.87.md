# PotatoFlow v1.6.87

- 加固任务状态并发更新、worker 租约、暂停、恢复与批量重试，避免完成、暂停和排队状态被旧 worker 覆盖。
- 统一烧录、AI 简介与投稿排队状态，失败阶段优先于残留排队标记。
- 将单任务删除、批量清空、录播任务删除和封面替换改为可回滚流程，数据库失败时恢复原文件。
- 修复录制引擎启停、人工审核继续、B站换源、设置保存和监控后台线程启动失败后的残留状态。
- 加固线上稿件元数据同步、本地检查点和漏单恢复的一致性。
