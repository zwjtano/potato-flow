# PotatoFlow v1.6.26

## Windows 修复

- 修复安装版启动录制核心时弹出 `biliup.exe` 控制台窗口的问题。
- 修复安装版忽略 `bin\ffmpeg.exe`、转而尝试写入只读 `Program Files` 目录，导致编码器检测失败的问题。
- FFmpeg 自动下载仅在所有现有候选均不可用时执行，并保存到可写的用户数据目录。
- 本机实测可正确推荐 AMD 780M：`h264_amf / balanced / CQP 20`，同时保留 `libx264 / medium / CRF 20` 回退。
