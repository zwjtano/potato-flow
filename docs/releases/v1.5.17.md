## 修复

- 录播分片只读取文件名与录制目录匹配的 XML，避免误用下一段录制的弹幕时间。
- 没有匹配 XML 时，从录制目录名解析北京时间，并使用一小时录制窗口。
- 多场 DOTA 2 对局与录制窗口重叠时，选择重叠时间最长的对局，不再无条件取最后一局。
- 修复因此可能导致的主播英雄、装备、KDA 以及两种比例封面取错对局的问题。
- 使用 Dota 2 官方简体中文物品数据补齐斗鱼映射中的空名称和缺失物品；`Duelist Gloves` 现在显示为“决斗家手套”，其他当前装备也会自动同步中文名。
- 新增完整的 Dota 2 装备中文名、英文名、物品 ID、内部名与官方图片 Markdown 对照表。

## 验证

- 新增错位 XML、目录时间回退和最长重叠对局三项回归测试。
- 新增官方中文物品名补全测试。
- 本次相关测试全部通过。
- Python 语法检查与 Git 差异检查通过。
