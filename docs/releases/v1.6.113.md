# v1.6.113

- 录播投稿自动章节优先采用 PotatoFlow 已验证的简介时间线，时间点不足或保存失败时再调用 B站原生 AI。
- 已发布的录播任务详情新增“生成章节”菜单，可手动选择“按简介时间线生成”或“用 B站 AI 生成”，便于直接对比两种结果。
- 每个直播间可预先选择自动、仅简介时间线、仅 B站 AI 或关闭自动章节，不必等录制开始后再设置。
- 章节写入会校验至少两个有效时间点、首章从 00:00 开始，并根据视频时长生成连续区间。

本版本为服务端与源码更新，不触发 Windows / macOS 桌面构建。
