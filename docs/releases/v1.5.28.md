# PotatoFlow v1.5.28

- 已发布录播的“重新生成简介”现在与自动投稿共用 XML 弹幕证据链，程序回到完整 XML 校验并生成可点击时间点。
- 无法生成通过 XML 校验的时间点时，保留原简介，不再用无时间版本覆盖已发布稿件。
- 重新生成的简介会按现有设置将该场直播统计置于开头，包括斗鱼付费“高能弹幕”的数量与金额。
- 兼容早期任务：阶段记录中没有统计摘要时，会尝试从录制目录重新读取直播数据。
- 模型返回的“重要事件”文字列表会在程序生成可校验时间点前移除，避免出现一份无时间、一份有时间的重复事件列表。
